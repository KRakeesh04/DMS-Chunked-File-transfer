
▶ Consumer (download, verify, merge) 💻
python3 dms_chunk_sync.py --mode consumer

▶ Producer (VPS: download, split, upload) 🔧
python3 dms_chunk_sync.py --mode producer

🔁 Local merge (verify checksums and merge groups by base filename)
python3 merge_chunks.py --manifest manifest.txt --chunks-dir chunks --output-dir .

Quick merge (defaults)
python3 merge_chunks.py

⚙️ Recommended setup (virtual environment + requests)
python3 -m venv venv
source venv/bin/activate
pip install requests

ℹ️ Notes:
- Ensure `manifest.txt` lists only the chunks you want to merge.
- `merge_chunks.py` will verify SHA256 checksums before merging and skip groups with mismatches.


